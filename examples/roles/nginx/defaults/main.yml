---
# Настройки роли "nginx" (reverse proxy + кеширование + раздача статики
# контейнерного приложения). Значения по умолчанию здесь; конфигурация
# конкретного проекта задается в vars/main.yml (у него выше приоритет).

# Пакет и сервис
nginx_package_name: "nginx"
nginx_service_state: started
nginx_service_enabled: true

# Пути (переопределяются в vars/<os>.yml)
nginx_conf_file_path: /etc/nginx/nginx.conf
nginx_mime_file_path: /etc/nginx/mime.types
nginx_pidfile: /run/nginx.pid
nginx_conf_path: /etc/nginx/conf.d
nginx_vhost_path: /etc/nginx/sites-enabled
nginx_default_vhost_path: /etc/nginx/sites-enabled/default
nginx_root_group: root
nginx_user: "www-data"

# Основные параметры nginx.conf
nginx_worker_processes: >-
  "{{ ansible_facts.processor_vcpus | default(ansible_facts.processor_count) }}"
nginx_worker_connections: "1024"
nginx_multi_accept: "off"
nginx_error_log: "/var/log/nginx/error.log warn"
nginx_access_log: "/var/log/nginx/access.log main buffer=16k flush=2m"
nginx_sendfile: "on"
nginx_tcp_nopush: "on"
nginx_tcp_nodelay: "on"
nginx_keepalive_timeout: "75"
nginx_keepalive_requests: "600"
nginx_server_tokens: "on"
nginx_client_max_body_size: "128m"
nginx_server_names_hash_bucket_size: "64"
nginx_listen_ipv6: true

nginx_log_format: |-
  '$remote_addr - $remote_user [$time_local] "$request" '
  '$status $body_bytes_sent "$http_referer" '
  '"$http_user_agent" "$http_x_forwarded_for"'

# Доп. опции внутри http {} — здесь включён gzip для текстовых ответов.
nginx_extra_http_options: |
  gzip on;
  gzip_comp_level 5;
  gzip_min_length 256;
  gzip_proxied any;
  gzip_vary on;
  gzip_types
    text/plain
    text/css
    text/js
    text/xml
    application/javascript
    application/json
    application/xml
    application/rss+xml
    image/svg+xml;

# Доп. опции внутри main context {} (по умолчанию пусто).
nginx_extra_conf_options: ""

# Удалить дефолтный vhost дистрибутива.
nginx_remove_default_vhost: true

# --- Приложение ---------------------------------------------------------------
# Каталоги, которые контейнер расшаривает на хост volume-ами:
#   volumes:
#     - ./public:/srv/app/public   # собранная статика фронта
#     - ./media:/srv/app/media     # пользовательские загрузки
nginx_app_static_root: "/srv/app/public"
nginx_app_media_root: "/srv/app/media"

# Прокси-кеш: путь, зона (16м ~ 140 тыс. ключей), макс. размер, TTL неиспользуемых.
# Собирается из простых переменных nginx_proxy_cache_* (см. vars/main.yml).
nginx_proxy_cache_dir: "/var/cache/nginx/proxy"
nginx_proxy_cache_max_size: "2g"
nginx_proxy_cache_inactive: "60m"
nginx_proxy_cache_valid_200: "10m"
nginx_proxy_cache_valid_404: "1m"
nginx_proxy_cache_path: >-
  {{ nginx_proxy_cache_dir }} levels=1:2 keys_zone=app_cache:16m
  max_size={{ nginx_proxy_cache_max_size }} inactive={{ nginx_proxy_cache_inactive }}
  use_temp_path=off

# Upstream на бэкенд-контейнер и виртуальные хосты по умолчанию.
# В реальных проектах переопределяются в vars/main.yml (у vars выше приоритет).
nginx_upstreams:
  - name: app_backend
    keepalive: 32
    servers:
      - "backend:8080"

nginx_vhosts: []
